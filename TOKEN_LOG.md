# Token Log

Tracks token usage at a consolidated, per-task level for later review and process
improvement. One row per logically complete unit of work — not per tool call or
message. See `~/.claude/CLAUDE.md` for the full convention.

| Date       | Task / Feature                                                                             | Est. Tokens                                                                                         | Context Size (approx) | Cost Drivers                                                                                                                                                                                                                                                                                                                                                                       | Notes / Improvement Ideas                                                                                                                                                                                                                                                                             |
| ---------- | ------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-22 | Add Linear issues integration to sidebar (full parity w/ GitHub: list + detail + comments) | ~183k (orchestrating session); implementation subagent separately reported ~199k on its own context | large                 | Explore agent mapped the existing GitHub integration first (no shared abstraction existed, so had to document the real pattern before designing Linear's). Bulk of the implementation subagent's cost was reading the full GitHub issues view end-to-end (components, hooks, types, settings API-key UI) before writing anything, to mirror conventions exactly rather than guess. | Delegating the read-heavy discovery (Explore) and the read-heavy implementation (clean-code-architect subagent) to separate agents kept this session's own context small relative to the total work done — worth repeating for similarly large "mirror an existing feature for a new provider" tasks. |

<!-- See ~/.claude/TOKEN_LOG-template.md for the full field guide. -->
